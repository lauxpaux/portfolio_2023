window.alert("connected")

const moon = document.querySelector("button img")

let toggle = false;


moon.addEventListener("click", () => {
    const darkModeTimeline = anime.timeline({

        timeline.add({
            targets: "body",
            backgroundColor: toggle ?
        })
    })
})